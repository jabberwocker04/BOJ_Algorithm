package today_solving;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.StringTokenizer;


public class Main_9372 { // 그래프 문제

	public static void main(String[] args) throws Exception {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringTokenizer st = new StringTokenizer(br.readLine());
		
		int Testcase = Integer.parseInt(st.nextToken());
		for(int i=0; i<Testcase; i++) { // testcase 
			st = new StringTokenizer(br.readLine()); // N(국가) M(비행기) 종류 입력
			int a = Integer.parseInt(st.nextToken()); // 국가
			int b = Integer.parseInt(st.nextToken()); // 비행기
			for(int j=0; j<b; j++) { // 
				List
			}
		}
		
	}

}
